---
title: sqlguard
description: SQL safety analysis engine for PHP. It does not write SQL, it judges it.
---

`sqlguard` does not write SQL. It **judges** the SQL that already exists.

Most pages are not translated yet: Starlight falls back to French automatically rather than
serving a missing page.
